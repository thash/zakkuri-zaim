<div id=<%= type %> class="modal hide fade">
  <div class="modal-header">
    <a class="close" data-dismiss="modal">×</a>
    <h3><%= "[ #{name} ]の値段を選択" %></h3>
  </div>
  <div class="modal-body">
    <%= image_tag "#{type}.png", {alt: name, width: 128, height: 128} %>
    <div>
      <% for price in prices do %>
        <a href="#" class="btn btn-primary"><%= "約#{price}円" %></a>
      <% end %>
    </div>
  </div>
</div>

<div class="hidden">
  このふぉーむをmodalに適用するよ
  <%= form_tag zaim_pay_url do %>
    <%= text_field_tag :category_id, "101" %>
    <%= text_field_tag :genre_id, "10101" %>
    <%= text_field_tag :price, "-500" %>
    <%= submit_tag "送信" %>
  <% end %>
</div>
